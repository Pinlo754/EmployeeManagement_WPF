<Window x:Class="EmployeeManagement.NotificationFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Soạn thông báo" Height="400" Width="450"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Tiêu đề:" VerticalAlignment="Center" Margin="5"/>
        <TextBox x:Name="TitleTextBox" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Gửi đến:" Grid.Row="1" VerticalAlignment="Center" Margin="5"/>
        <ComboBox x:Name="DepartmentComboBox" Grid.Row="1" Grid.Column="1" Margin="5"
                  DisplayMemberPath="DepartmentName"
                  SelectedValuePath="DepartmentId" />

        <TextBlock Text="Nội dung:" Grid.Row="2" Margin="5" Padding="0,5,0,0"/>
        <TextBox x:Name="MessageTextBox" Grid.Row="2" Grid.Column="1" Margin="5"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="SaveButton" Content="Lưu" Width="80" Height="30" Margin="5" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Hủy" Width="80" Height="30" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>