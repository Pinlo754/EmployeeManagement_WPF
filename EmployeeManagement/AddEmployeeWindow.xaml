<Window x:Class="EmployeeManagement.AddEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm nhân viên" Height="800" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Shadow effect -->
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="8" Color="#888" ShadowDepth="2"/>

        <!-- Style Button hiện đại -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Effect="{StaticResource ShadowEffect}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004C87"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style Cancel button -->
        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E74C3C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C0392B"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A93226"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho TextBox và ComboBox -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Họ tên" FontWeight="Bold"/>
                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Ngày sinh" FontWeight="Bold"/>
                <DatePicker SelectedDate="{Binding DateOfBirthPicker}"/>

                <TextBlock Text="Giới tính" FontWeight="Bold"/>
                <ComboBox SelectedValue="{Binding Gender, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content">
                    <ComboBoxItem Content="Nam"/>
                    <ComboBoxItem Content="Nữ"/>
                </ComboBox>

                <TextBlock Text="Địa chỉ" FontWeight="Bold"/>
                <TextBox Text="{Binding Address}"/>

                <TextBlock Text="Số điện thoại" FontWeight="Bold"/>
                <TextBox Text="{Binding Phone}"/>

                <TextBlock Text="Email" FontWeight="Bold"/>
                <TextBox Text="{Binding Email}"/>

                <TextBlock Text="Phòng ban" FontWeight="Bold"/>
                <ComboBox ItemsSource="{Binding Departments}" 
                          SelectedItem="{Binding SelectedDepartment}" 
                          DisplayMemberPath="DepartmentName" 
                          SelectedValuePath="DepartmentId"/>

                <TextBlock Text="Chức vụ" FontWeight="Bold"/>
                <TextBox Text="{Binding Position}"/>

                <TextBlock Text="Lương cơ bản" FontWeight="Bold"/>
                <TextBox Text="{Binding BaseSalary}"/>

                <TextBlock Text="Ngày bắt đầu" FontWeight="Bold"/>
                <DatePicker SelectedDate="{Binding StartDatePicker}"/>

                <TextBlock Text="Avatar URL" FontWeight="Bold"/>
                <TextBox Text="{Binding AvatarUrl}"/>

                <TextBlock Text="Username" FontWeight="Bold"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Password" FontWeight="Bold"/>
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0" Width="220">
            <Button Content="Hủy" Style="{StaticResource CancelButtonStyle}" Click="Cancel_Click" Width="97"/>
            <Button Content="Lưu" Style="{StaticResource ModernButton}" Click="Save_Click" Width="106"/>
        </StackPanel>
    </Grid>
</Window>
