<Window x:Class="EmployeeManagement.DepartmentFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Phòng ban" Height="280" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Tên phòng ban:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtName" Height="30" Margin="0,0,0,10"/>

            <TextBlock Text="Mô tả:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtDescription" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Lưu" Width="80" Margin="5" Background="#28A745" Foreground="White" Click="Save_Click"/>
            <Button Content="Hủy" Width="80" Margin="5" Background="#6C757D" Foreground="White" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
